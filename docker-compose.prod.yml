services:
  workstacean:
    build:
      context: .
      target: release
    restart: unless-stopped
    env_file: .env
    environment:
      - WORKSPACE_DIR=/workspace
      - DATA_DIR=/data
    volumes:
      - workspace:/workspace
      - data:/data

volumes:
  data:
  workspace:
